apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespaces.yaml
  - metallb.yaml
  - kafka-local.yaml
  - storage.yaml
  - spark-deployment.yaml
  - trino-deployment.yaml
  - metabase-deployment.yaml
  - superset-deployment.yaml
  - airflow-deployment.yaml
  - python-processor-deployment.yaml

configMapGenerator:
  - name: platform-config
    namespace: kafka
    literals:
      - KAFKA_BROKER=kafka-service.kafka.svc.cluster.local:9092
      - ZOOKEEPER_BROKER=zookeeper-service.infrastructure.svc.cluster.local:2181
      - MINIO_ENDPOINT=minio-service.infrastructure.svc.cluster.local:9000
      - TRINO_ENDPOINT=trino-service.analytics.svc.cluster.local:8080
      - ENVIRONMENT=local
      - LOG_LEVEL=DEBUG

